#**
    The value of 'payload' after the template has been evaluated
    will be passed as the event to AWS Lambda.
*#
{
    "operation": "Invoke",
    "payload": {
    	"endpoint":"/event/${ctx.args.eventKey}/simple"
    }
}